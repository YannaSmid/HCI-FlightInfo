using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SelectionHandler : MonoBehaviour
{
    //keeps track of the objects that are selected and the objects that still can be selected

    //routes
    public bool RouteisSelected = false;
    public bool SelectingRoute = false;

    //deselecting
    public bool Deselecting = false;

    //buttons
    public bool ButtonSelection = false;

    

    public void HandleSelection(){

    }
}
